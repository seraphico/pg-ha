# Rust build artifacts
/target/
**/target/

# Cargo lock for libraries (keep for binaries)
# Cargo.lock is committed for this project since it produces binaries

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.Cargo.lock
# OS
.DS_Store
Thumbs.db

# Environment
.env
*.env.local

# Kiro specs (local dev tooling)
.kiro/

# Proptest regression files
**/proptest-regressions/

# TLS certificates (local dev only — do not commit private keys)
docker/tls/*.key
docker/tls/*.srl
